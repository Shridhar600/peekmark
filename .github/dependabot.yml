version: 2
updates:
  # Keep GitHub Actions used by CI up to date. (Swift package deps aren't tracked
  # here because the Xcode project / Package.resolved is generated, not committed.)
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
